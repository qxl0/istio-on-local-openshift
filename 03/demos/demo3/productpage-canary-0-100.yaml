apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: bookinfo
spec:
  hosts:
    - bbookinfo-istio-gateway.apps-crc.testing
  gateways:
    - bookinfo-gateway
  http:
  - route:
    - destination:
        host: productpage
        subset: v1
        port:
          number: 9080
      weight: 0
    - destination:
        host: productpage
        subset: v2
        port:
          number: 9080
      weight: 100