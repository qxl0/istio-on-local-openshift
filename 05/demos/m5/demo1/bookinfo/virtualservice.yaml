apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  namespace: bookinfo
  name: bookinfo
spec:
  hosts:
  - bookinfo.local
  - bookinfo-istio-gateway.apps-crc.testing
  gateways:
  - istio-gateway/gateway
  exportTo:
  - istio-gateway
  http:
  - route:
    - destination:
        host: productpage
        port:
          number: 9080
