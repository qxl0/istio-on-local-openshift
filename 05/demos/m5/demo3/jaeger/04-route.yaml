apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: jaeger
  namespace: istio-gateway
spec:
  host: jaeger-istio-gateway.apps-crc.testing
  to:
    kind: Service
    name: ingressgateway
  port:
    targetPort: http2
